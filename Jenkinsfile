pipeline {
    agent any

    stages {
     stage('Check npm') {
                steps {
                    sh 'which npm || echo "npm non installé"'
                }
            }
        stage('Check npm') {
            steps {
                sh 'npm -v'
            }
        }
    }
}
